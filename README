What is this?
- This is 2D Massively Multiplayer Online Role-Playing Game

What do I need to compile and run this?

(client and server):
- boost: http://www.boost.org/

(client only):
- SFML: https://github.com/LaurentGomila/SFML
- SFGUI: http://sfgui.sfml-dev.de/download

(server only):
- MySQL C connector: http://dev.mysql.com/downloads/connector/c/
- MySQL C++ connector (NOTE: You should compile from source on Windows): http://dev.mysql.com/downloads/connector/cpp/
- MySQL server: http://dev.mysql.com/downloads/mysql/

Specials instructions:
    For profilage mode, using gprof:
            - go in build folder
            - cmake . -DPROFILE_MODE=TRUE -DCMAKE_BUILD_TYPE=Debug
            - make
            - launch client in debug mode
            - launch server in debug mode
            - play one minutes
            - close them
            - in client debug directory, gprof client_exe_name > client_profile.txt
            - in server debug directory, gprof server_exe_name > server_profile.txt


